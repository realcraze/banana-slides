services:
  oauth2-proxy:
    image: quay.io/oauth2-proxy/oauth2-proxy:v7.12.0
    container_name: banana-slides-oauth2-proxy-local
    ports:
      - "${OAUTH2_PROXY_PORT:-4180}:4180"
    command:
      - --provider=entra-id
      - --http-address=0.0.0.0:4180
      - --redirect-url=http://localhost:${OAUTH2_PROXY_PORT:-4180}/oauth2/callback
      - --upstream=http://host.docker.internal:${FRONTEND_PORT:-3051}
      - --client-id=${OAUTH2_PROXY_CLIENT_ID}
      - --client-secret=${OAUTH2_PROXY_CLIENT_SECRET}
      - --cookie-secret=${OAUTH2_PROXY_COOKIE_SECRET}
      - --email-domain=${OAUTH2_PROXY_EMAIL_DOMAIN}
      - --oidc-issuer-url=https://login.microsoftonline.com/${OAUTH2_PROXY_TENANT_ID}/v2.0
      - --scope=openid profile email User.Read
      - --cookie-secure=false
      - --session-cookie-minimal=true
      - --pass-access-token=false
      - --pass-authorization-header=false
      - --pass-user-headers=true
      - --pass-basic-auth=false
      - --skip-provider-button=true
      - --skip-auth-route=GET=^/$
      - --skip-auth-route=GET=^/[^/]*$
      - --skip-auth-route=GET=^/assets/.*
      - --skip-auth-route=GET=^/@vite/.*
      - --skip-auth-route=GET=^/src/.*
      - --skip-auth-route=GET=^/node_modules/.*
      - --skip-auth-route=GET=^/@react-refresh.*
    restart: unless-stopped
